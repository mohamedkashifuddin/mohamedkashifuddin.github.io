source "https://rubygems.org"

# This gem is required to build on GitHub Pages.
gem "github-pages", group: :jekyll_plugins

# If you have any other plugins, put them here!
# gem "jekyll-seo-tag" # This is already part of github-pages gem
# gem "jekyll-feed"
# gem "jekyll-sitemap"

# Minimal theme is included with github-pages gem.
# No need to explicitly list it here if you are using 'theme: jekyll-theme-minimal' in _config.yml.

# Optional: If you want to use a specific Jekyll version, uncomment this.
# gem "jekyll", "~> 4.3" # or whatever version you prefer, but let github-pages manage it usually